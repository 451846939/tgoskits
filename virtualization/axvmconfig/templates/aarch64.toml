[base]
id = 1
name = "GuestVM-aarch64"
guest_type = "passthrough"
cpu_num = 1
phys_cpu_sets = [1]

[kernel]
entry_point = 0x4008_0000
kernel_path = "arceos-aarch64.bin"
kernel_load_addr = 0x4008_0000
image_location = "fs"
memory_regions = [
    [0x8000_0000, 0x4000_0000, 0x7, 1],
]

# PL011 and the virtual interrupt controller are supplied by the machine profile.
[devices]
passthrough = []
disabled = []
